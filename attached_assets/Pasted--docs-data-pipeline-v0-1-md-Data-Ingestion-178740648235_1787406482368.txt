Создай новый документ:

docs/data-pipeline-v0.1.md

Задача:
Зафиксировать архитектуру Data Ingestion Pipeline для проекта "ОценитьКвартиру.рф".

ВАЖНО:
- Создать только этот файл.
- Не менять код.
- Не менять модели БД.
- Не менять миграции.
- Не создавать другие файлы.

Документ должен описывать текущий архитектурный дизайн Task #4.

Структура документа:

# Data Pipeline Architecture v0.1

## 1. Purpose

Описание назначения pipeline:

Pipeline отвечает за получение данных об объектах недвижимости из внешних источников, сохранение исходных данных, нормализацию и подготовку данных для последующей оценки стоимости.

---

## 2. Data Flow

Описать поток:

External Source
        |
        ↓
Source Adapter
        |
        ↓
Raw Listing Storage
        |
        ↓
Normalizer
        |
        ↓
Building / Property / Listing
        |
        ↓
Comparable Engine


---

## 3. Architectural Principles

Зафиксировать:

### Raw data is immutable

Таблица raw_listings является неизменяемым журналом входящих данных.

После сохранения:
- raw_data не изменяется;
- сохраняется внешний идентификатор источника;
- сохраняется время получения данных.

---

### Normalized data

Нормализованные сущности:

Building:
- физическое здание;
- адрес;
- координаты;
- характеристики дома.

Property:
- конкретный объект недвижимости;
- площадь;
- комнаты;
- этаж.

Listing:
- объявление конкретного источника;
- цена;
- статус;
- ссылка на источник.

---

## 4. Source Adapter Layer

Описать назначение:

Source Adapter скрывает различия между источниками данных.

Каждый источник должен иметь единый интерфейс:

- collect()
- parse()
- normalize()

Примеры реализаций:

- MockAdapter
- CIANAdapter (future)
- AvitoAdapter (future)
- YandexAdapter (future)


---

## 5. Normalization Strategy

Описать:

RawListing
        ↓
Normalizer
        ↓
Building
        ↓
Property
        ↓
Listing


Правила:

Building:
- идентификация по адресу и координатам;
- дубликаты не создаются при повторной загрузке.

Property:
- связан с Building;
- описывает физический объект недвижимости.

Listing:
- каждое объявление источника хранится отдельно;
- история изменений сохраняется.


---

## 6. Data Ownership

Добавить таблицу:

| Layer | Owner | Storage |
|------|------|---------|
| Raw data | Collector | raw_listings |
| Building identity | Normalizer | buildings |
| Property identity | Normalizer | properties |
| Market offer | Source Adapter + Normalizer | listings |


---

## 7. Current Implementation Status

Зафиксировать:

Completed:
- PostgreSQL/PostGIS database
- SQLAlchemy models
- Alembic migrations

Planned:
- Source Adapter Interface
- MockAdapter
- Collector
- Normalizer
- External sources integration


---

## 8. Out of Scope

Зафиксировать:

Task #4 не включает:

- реальные scraper реализации;
- подключение ЦИАН/Авито;
- оценку стоимости;
- Comparable Engine;
- пользовательский API;
- UI.


После создания:
1. Проверить Markdown.
2. Показать git diff только нового файла.
3. Не делать commit.